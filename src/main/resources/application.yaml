spring:
  datasource:
    #url: jdbc:mysql://localhost:3306/ktb_week4?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    #username: admin
    #password: 1234
    #driver-class-name: com.mysql.cj.jdbc.Driver
   url: jdbc:h2:tcp://localhost/~/test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;NON_KEYWORDS=VALUE
   username: sa
   password:
   driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

#logging:
#  level:
#    org.hibernate.SQL: debug